language: node_js

node_js:
  - "stable"

services:
  - postgresql

before_script:
  - psql -c 'create database shopmate-backend-test;' -U postgres
  - psql -c "CREATE USER postgres WITH PASSWORD 'postgres';" -U postgres
